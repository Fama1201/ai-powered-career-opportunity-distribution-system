# Force Hibernate to create tables from scratch for tests
spring.jpa.hibernate.ddl-auto=create-drop

# Use the standard PostgreSQL dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect